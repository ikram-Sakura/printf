#include "main.h"

/**
 * length_modifiers - unsigned int
 * @n : number
 * Return: count
 */

int	length_modifiers(unsigned int n)
{
	int	count;

	count = 0;
	count += ft_pointer(n);
	return (count);
}